<div class="center jumbotron">
  <h1><%= t "static_pages.welcome" %></h1>
  <h2><%= t"static_pages.home_page" %></h2>
  <% if !user_signed_in? %>
    <%= link_to t("signup.signup_now"), new_user_registration_path,
      class: "btn btn-lg btn-primary" %>
  <% end %>
</div>
